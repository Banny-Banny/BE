# 🎤 기술 발표 자료 - Banny-Banny Backend

> **프로젝트**: 타임캡슐 서비스 백엔드 API
> **작성자**: 김동언
> **작성일**: 2026-01-11

---

## 📚 목차

1. [결제-캡슐 생성 트랜잭션 & 보상 처리](#1-결제-캡슐-생성-트랜잭션--보상-처리-)
2. [Step Room 시스템 - 대기실 참여 및 상태 관리](#2-step-room-시스템---대기실-참여-및-상태-관리)
3. [Cron 기반 타임캡슐 자동 제출](#3-cron-기반-타임캡슐-자동-제출)
4. [위치 기반 이스터에그 검색 (Haversine 공식)](#4-위치-기반-이스터에그-검색-haversine-공식)
5. [주문 금액 계산 로직](#5-주문-금액-계산-로직)

---

# 1. 결제-캡슐 생성 트랜잭션 & 보상 처리 ⭐

## 🎯 기술적 도전 과제

### 문제 상황
- **외부 PG사 (토스페이먼츠) API 호출**과 **내부 DB 트랜잭션**을 안전하게 연계
- PG 승인은 성공했으나 DB 저장 실패 시 → **자동 환불 처리 필요**
- 결제 완료 후 **캡슐 생성 실패 시 보상 트랜잭션** 구현

### 해결 방법
- **2단계 커밋 패턴** 변형 적용
- Try-Catch-Compensate 패턴으로 보상 트랜잭션 구현

---

## 📊 플로우 차트

\`\`\`
[클라이언트 결제 요청]
         ↓
[1. 주문 검증 (OrderStatus, Amount)]
         ↓
[2. 토스페이먼츠 API 승인 요청]
         ↓
    ┌────┴────┐
    │ 성공?   │
    └────┬────┘
         ↓ YES
[3. DB 트랜잭션 시작]
         ↓
[3-1. Payment 엔티티 저장 (status=PAID)]
[3-2. Order 상태 업데이트 (status=PAID)]
         ↓
    ┌────┴────┐
    │ 성공?   │
    └────┬────┘
         ↓ YES
[4. 캡슐 생성 (Step Room)]
         ↓
    ┌────┴────┐
    │ 성공?   │
    └────┬────┘
         ↓ YES
[5. 응답 반환 (capsule_id, invite_code)]
         ↓
         [완료]

    ↓ NO (3, 4단계 실패 시)
[🔄 보상 트랜잭션]
         ↓
[토스페이먼츠 환불 API 호출]
         ↓
[에러 로그 기록]
         ↓
[에러 응답 반환]
\`\`\`

---

## 🔑 핵심 코드 1: 메인 트랜잭션 흐름

**파일**: `src/payments/payments.service.ts`

\`\`\`typescript
async tossConfirm(user: User, dto: TossConfirmDto) {
  // 1️⃣ 주문 검증
  const order = await this.orderRepository.findOne({
    where: { id: dto.orderId },
    relations: { product: true },
  });
  
  if (!order) throw new NotFoundException('ORDER_NOT_FOUND');
  if (order.userId !== user.id) throw new UnauthorizedException('ORDER_NOT_OWNED');
  if (order.status === OrderStatus.PAID) throw new BadRequestException('ORDER_ALREADY_PAID');
  if (order.totalAmount !== dto.amount) throw new BadRequestException('AMOUNT_MISMATCH');

  // 2️⃣ PG 승인 요청 (토스페이먼츠 API)
  const tossRes = await this.callTossConfirm(dto);

  let payment: Payment;
  let capsule: Capsule;

  try {
    // 3️⃣ DB 트랜잭션 (결제 정보 저장)
    payment = await this.dataSource.transaction(async (manager) => {
      let pay = await manager.getRepository(Payment).findOne({
        where: { orderId: order.id },
      });
      
      if (!pay) {
        pay = manager.getRepository(Payment).create({
          orderId: order.id,
          pgTid: tossRes.paymentKey ?? crypto.randomUUID(),
          amount: order.totalAmount,
          status: PaymentStatus.READY,
        });
      }

      pay = this.mapTossPaymentToEntity(pay, tossRes, dto.orderId);
      pay.status = PaymentStatus.PAID;

      await manager.getRepository(Payment).save(pay);
      await manager.getRepository(Order).save({
        ...order,
        status: OrderStatus.PAID,
      });

      return pay;
    });

    // 4️⃣ 캡슐 생성 (Step Room)
    capsule = await this.stepRoomService.createCapsuleWithStepRoom(order.id);
    
  } catch (error) {
    // 🔄 보상 트랜잭션: PG 환불 처리
    if (tossRes?.paymentKey) {
      try {
        await this.callTossCancel({
          paymentKey: tossRes.paymentKey,
          cancelReason: '결제 처리 실패 (서버 오류)',
          cancelAmount: order.totalAmount,
        });
      } catch (cancelError) {
        // 환불 실패 시 로그 기록 (실제로는 알림/모니터링 필요)
        console.error('보상 트랜잭션(환불) 실패:', cancelError);
      }
    }
    throw error;
  }

  // 5️⃣ 성공 응답
  return {
    order_id: order.id,
    payment_key: payment.paymentKey,
    status: OrderStatus.PAID,
    capsule_id: capsule.id,
    step_room: {
      room_id: capsule.id,
      invite_code: capsule.inviteCode,
      // ...
    },
  };
}
\`\`\`

---

## 🔑 핵심 코드 2: 토스페이먼츠 API 호출

\`\`\`typescript
private async callTossConfirm(dto: TossConfirmDto) {
  console.log('🔵 [Toss Confirm] 승인 요청 시작:', {
    orderId: dto.orderId,
    amount: dto.amount,
  });

  if (this.useTossMock) {
    // Mock 모드: 즉시 성공 응답
    return {
      paymentKey: dto.paymentKey,
      orderId: dto.orderId,
      status: 'DONE',
      approvedAt: new Date().toISOString(),
      // ...
    };
  }

  // 실제 API 호출
  const url = \`\${this.tossBaseUrl}/v1/payments/confirm\`;
  const res = await fetch(url, {
    method: 'POST',
    headers: {
      Authorization: this.getTossAuthHeader(), // Basic Auth
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      paymentKey: dto.paymentKey,
      orderId: dto.orderId,
      amount: dto.amount,
    }),
  });

  if (!res.ok) {
    const text = await res.text();
    throw new BadRequestException(\`TOSS_CONFIRM_FAILED: \${text}\`);
  }

  return await res.json();
}
\`\`\`

---

## 🔑 핵심 코드 3: 보상 트랜잭션 (환불)

\`\`\`typescript
private async callTossCancel(dto: TossCancelDto) {
  if (this.useTossMock) {
    return {
      paymentKey: dto.paymentKey,
      status: 'CANCELED',
      cancels: [{
        cancelReason: dto.cancelReason,
        cancelAmount: dto.cancelAmount,
        canceledAt: new Date().toISOString(),
      }],
    };
  }

  const url = \`\${this.tossBaseUrl}/v1/payments/\${dto.paymentKey}/cancel\`;
  const res = await fetch(url, {
    method: 'POST',
    headers: {
      Authorization: this.getTossAuthHeader(),
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      cancelReason: dto.cancelReason,
      cancelAmount: dto.cancelAmount,
    }),
  });

  if (!res.ok) {
    const text = await res.text();
    throw new BadRequestException(\`TOSS_CANCEL_FAILED: \${text}\`);
  }

  return await res.json();
}
\`\`\`

---

## 💡 기술적 인사이트

### 1. **분산 트랜잭션의 한계**
- 외부 API(PG)와 DB는 같은 트랜잭션으로 묶을 수 없음
- **Saga 패턴** 변형으로 보상 트랜잭션 구현

### 2. **멱등성 보장**
- 동일한 `orderId`로 중복 승인 요청 시 에러 처리
- `ORDER_ALREADY_PAID` 체크로 중복 결제 방지

### 3. **로그 및 모니터링**
- 각 단계마다 상세 로그 기록 (`console.log`)
- 보상 트랜잭션 실패 시 별도 알림 필요 (TODO)

### 4. **에러 핸들링 전략**
- PG 승인 실패: 즉시 에러 반환 (클라이언트 재시도 가능)
- DB 저장 실패: 자동 환불 후 에러 반환
- 환불 실패: 로그 기록 + 관리자 수동 처리 필요

---

## 📈 시퀀스 다이어그램

\`\`\`
클라이언트          API Server       TossPayments      Database
    |                  |                  |                |
    |-- POST /confirm ->|                  |                |
    |                  |-- 주문 검증 ----->|                |
    |                  |                  |                |
    |                  |-- 승인 요청 ----->|                |
    |                  |<-- 승인 완료 -----|                |
    |                  |                  |                |
    |                  |------------ DB 트랜잭션 ---------->|
    |                  |                  |<-- 저장 완료 ---|
    |                  |                  |                |
    |                  |-- 캡슐 생성 ----->|-- INSERT ----->|
    |                  |                  |<-- 생성 완료 ---|
    |                  |                  |                |
    |<-- 200 OK -------|                  |                |
    |  (capsule_id)    |                  |                |

    [ 실패 시 ]
    |                  |                  |                |
    |                  |-- 승인 요청 ----->|                |
    |                  |<-- 승인 완료 -----|                |
    |                  |                  |                |
    |                  |------------ DB 트랜잭션 ---------->|
    |                  |                  |<-- 실패 ❌-----|
    |                  |                  |                |
    |                  |-- 환불 요청 ----->|                |
    |                  |<-- 환불 완료 -----|                |
    |                  |                  |                |
    |<-- 500 Error ----|                  |                |
\`\`\`

---

# 2. Step Room 시스템 - 대기실 참여 및 상태 관리

## 🎯 기술적 도전 과제

### 문제 상황
- **다중 사용자가 동시에 참여** 시 슬롯 배정 동시성 문제
- 초대 코드 기반 **접근 제어**
- 참여자별 **콘텐츠 작성 상태 추적**

### 해결 방법
- **Pessimistic Lock** 으로 슬롯 배정 동시성 제어
- 트랜잭션 내 슬롯 조회 및 배정

---

## 📊 플로우 차트

\`\`\`
[클라이언트 참여 요청]
         ↓
[1. 초대 코드 검증]
         ↓
    ┌────┴────┐
    │ 유효?   │
    └────┬────┘
         ↓ YES
[2. 마감시한 확인]
         ↓
    ┌────┴────┐
    │ 유효?   │
    └────┬────┘
         ↓ YES
[3. 트랜잭션 시작]
         ↓
[3-1. 슬롯 조회 (Pessimistic Write Lock)]
         ↓
[3-2. 이미 참여 중인지 확인]
         ↓
    ┌────┴────┐
    │ 참여중? │
    └────┬────┘
         ↓ NO
[3-3. 빈 슬롯 찾기]
         ↓
    ┌────┴────┐
    │ 있음?   │
    └────┬────┘
         ↓ YES
[3-4. 슬롯 배정 (userId 할당)]
         ↓
[3-5. 트랜잭션 커밋]
         ↓
[4. 응답 반환 (slot_number)]
\`\`\`

---

## 🔑 핵심 코드: 슬롯 배정 (Pessimistic Lock)

**파일**: `src/capsules/capsules-step-room.service.ts`

\`\`\`typescript
async joinStepRoom(
  capsuleId: string,
  userId: string,
  inviteCode: string,
): Promise<JoinStepRoomResponseDto> {
  return await this.dataSource.transaction(async (manager) => {
    // 1️⃣ 캡슐 조회
    const capsule = await manager.findOne(Capsule, {
      where: { id: capsuleId },
    });

    if (!capsule) {
      throw new NotFoundException('대기실을 찾을 수 없습니다');
    }

    // 2️⃣ 초대 코드 검증
    if (
      !capsule.inviteCode ||
      capsule.inviteCode.toUpperCase() !== inviteCode.toUpperCase()
    ) {
      throw new ForbiddenException('올바르지 않은 초대 코드입니다');
    }

    // 3️⃣ 마감시한 확인
    if (capsule.deadline && new Date() > capsule.deadline) {
      throw new ForbiddenException('참여 마감시한이 지났습니다');
    }

    // 4️⃣ 슬롯 조회 (Pessimistic Write Lock)
    const slotRepo = manager.getRepository(CapsuleParticipantSlot);
    const slots = await slotRepo.find({
      where: { capsuleId: capsule.id },
      order: { slotIndex: 'ASC' },
      // 🔒 동시성 제어: 다른 트랜잭션이 이 슬롯을 수정하지 못하도록 잠금
      lock: { mode: 'pessimistic_write' },
    });

    // 5️⃣ 이미 참여 중인지 확인
    const existingSlot = slots.find((s) => s.userId === userId);
    if (existingSlot) {
      throw new ConflictException('이미 참여 중입니다');
    }

    // 6️⃣ 빈 슬롯 찾기
    const emptySlot = slots.find((s) => s.userId === null);
    if (!emptySlot) {
      throw new ForbiddenException('정원이 초과되었습니다');
    }

    // 7️⃣ 슬롯 배정
    emptySlot.userId = userId;
    emptySlot.assignedAt = new Date();
    await slotRepo.save(emptySlot);

    // 8️⃣ 응답 반환
    return {
      success: true,
      room_id: capsule.id,
      slot_number: emptySlot.slotIndex + 1,
      joined_at: emptySlot.assignedAt,
    };
  });
}
\`\`\`

---

## 💡 기술적 인사이트

### 1. **동시성 제어 전략**
- **Pessimistic Lock** 사용 → 슬롯 조회 시 다른 트랜잭션 대기
- Race Condition 방지

### 2. **초대 코드 생성**
\`\`\`typescript
private generateInviteCode(): string {
  // 혼동되기 쉬운 문자 제외 (0, O, 1, I, L 등)
  const chars = '23456789ABCDEFGHJKMNPQRSTUVWXYZ';
  let code = '';
  for (let i = 0; i < 6; i++) {
    code += chars[Math.floor(Math.random() * chars.length)];
  }
  return code;
}
\`\`\`

### 3. **상태 관리**
- `RoomStatus`: WAITING → COMPLETED → BURIED
- 각 슬롯별 `status`: PENDING → COMPLETED

---

# 3. Cron 기반 타임캡슐 자동 제출

## 🎯 기술적 도전 과제

### 문제 상황
- **마감시한 경과 시 자동으로 캡슐 매장** 필요
- **개봉 시간 도래 시 알림 전송**

### 해결 방법
- **NestJS @nestjs/schedule** 패키지 사용
- 매시간 Cron 실행

---

## 📊 플로우 차트

\`\`\`
[매 시간 Cron 실행]
         ↓
[1. deadline 경과 캡슐 조회]
         ↓
    ┌────┴────┐
    │ 있음?   │
    └────┬────┘
         ↓ YES
[2. 각 캡슐 순회]
         ↓
[3. 트랜잭션 시작]
         ↓
[3-1. 기본 위치 설정 (서울시청)]
[3-2. roomStatus = BURIED]
[3-3. buriedAt = now]
[3-4. isAutoSubmitted = true]
         ↓
[3-5. 트랜잭션 커밋]
         ↓
[4. 알림 전송 (TODO)]
         ↓
[완료]
\`\`\`

---

## 🔑 핵심 코드

**파일**: `src/capsules/capsules-cron.service.ts`

\`\`\`typescript
@Injectable()
export class CapsulesCronService {
  private readonly logger = new Logger(CapsulesCronService.name);

  @Cron(CronExpression.EVERY_HOUR) // 매 시간 실행
  async handleAutoSubmit(): Promise<void> {
    this.logger.log('🕐 [크론잡 시작] 자동 제출 처리 시작');

    try {
      // 1️⃣ deadline 경과 + 미제출 캡슐 조회
      const now = new Date();
      const expiredCapsules = await this.capsuleRepository.find({
        where: {
          deadline: LessThan(now),
          roomStatus: In([RoomStatus.WAITING, RoomStatus.COMPLETED]),
          deletedAt: IsNull(),
        },
        relations: ['order'],
      });

      this.logger.log(\`✅ 자동 제출 대상 캡슐: \${expiredCapsules.length}개\`);

      if (expiredCapsules.length === 0) {
        return;
      }

      // 2️⃣ 각 캡슐 자동 매장
      for (const capsule of expiredCapsules) {
        try {
          await this.autoSubmitCapsule(capsule);
          this.logger.log(\`✅ 캡슐 자동 제출 완료: \${capsule.id}\`);
        } catch (error) {
          this.logger.error(\`❌ 캡슐 자동 제출 실패: \${capsule.id}\`, error);
        }
      }

      this.logger.log('🎉 [크론잡 완료] 자동 제출 처리 완료');
    } catch (error) {
      this.logger.error('❌ [크론잡 에러] 자동 제출 처리 중 오류 발생', error);
    }
  }

  private async autoSubmitCapsule(capsule: Capsule): Promise<void> {
    await this.dataSource.transaction(async (manager) => {
      const capsuleRepo = manager.getRepository(Capsule);

      // 기본 위치 설정 (서울시청)
      const latitude = 37.5665;
      const longitude = 126.978;

      capsule.latitude = latitude;
      capsule.longitude = longitude;
      capsule.roomStatus = RoomStatus.BURIED;
      capsule.buriedAt = new Date();
      capsule.isAutoSubmitted = true;

      await capsuleRepo.save(capsule);
    });
  }
}
\`\`\`

---

## 💡 기술적 인사이트

### 1. **Cron 표현식**
- `@Cron(CronExpression.EVERY_HOUR)`: 매 시간 실행
- 다른 옵션: `EVERY_DAY_AT_MIDNIGHT`, `EVERY_30_SECONDS` 등

### 2. **에러 핸들링**
- 개별 캡슐 처리 실패 시 다음 캡슐 계속 처리
- 전체 Cron 실패 시 로그 기록

### 3. **개봉 알림**
\`\`\`typescript
@Cron(CronExpression.EVERY_HOUR)
async handleCapsuleOpenNotifications(): Promise<void> {
  const now = new Date();
  const oneHourAgo = new Date(now.getTime() - 60 * 60 * 1000);

  const openedCapsules = await this.capsuleRepository.find({
    where: {
      openAt: Between(oneHourAgo, now),
      roomStatus: RoomStatus.BURIED,
      deletedAt: IsNull(),
    },
  });

  for (const capsule of openedCapsules) {
    await this.pushNotificationService.createAndSendNotification(
      capsule.userId,
      NotificationType.CAPSULE_OPEN,
      '캡슐이 열렸어요',
      \`\${capsule.title} 캡슐이 공개되었습니다.\`,
      { capsuleId: capsule.id },
    );
  }
}
\`\`\`

---

# 4. 위치 기반 이스터에그 검색 (Haversine 공식)

## 🎯 기술적 도전 과제

### 문제 상황
- **위도/경도 좌표 간 실제 거리 계산** 필요
- 반경 300m 이내 이스터에그 검색

### 해결 방법
- **Haversine 공식** 적용
- PostgreSQL 쿼리로 계산

---

## 📊 플로우 차트

\`\`\`
[클라이언트 위치 전송 (lat, lng)]
         ↓
[1. 위도/경도 검증]
         ↓
[2. SQL 쿼리 생성]
    - Haversine 공식 적용
    - 반경 300m 필터링
         ↓
[3. 거리 계산 실행]
         ↓
[4. 결과 반환]
    - distance_m 포함
\`\`\`

---

## 🔑 핵심 코드 1: Haversine 공식 (TypeScript)

**파일**: `src/capsules/capsules.service.ts`

\`\`\`typescript
private isWithinRadius(
  capsuleLat: number | null,
  capsuleLng: number | null,
  userLat?: number,
  userLng?: number,
  radiusMeters = 300,
): boolean {
  if (!capsuleLat || !capsuleLng) return true;
  if (userLat === undefined || userLng === undefined) return false;

  // 🌍 Haversine 공식
  const toRad = (deg: number) => (deg * Math.PI) / 180;
  const R = 6371e3; // 지구 반지름 (미터)

  const phi1 = toRad(capsuleLat);
  const phi2 = toRad(userLat);
  const dPhi = toRad(userLat - capsuleLat);
  const dLambda = toRad(userLng - capsuleLng);

  const a =
    Math.sin(dPhi / 2) * Math.sin(dPhi / 2) +
    Math.cos(phi1) *
      Math.cos(phi2) *
      Math.sin(dLambda / 2) *
      Math.sin(dLambda / 2);

  const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
  const distance = R * c;

  return distance <= radiusMeters;
}
\`\`\`

---

## 🔑 핵심 코드 2: PostgreSQL 쿼리

\`\`\`typescript
private buildDistanceExpr(lat: number, lng: number) {
  const latRad = (lat * Math.PI) / 180;
  const lngRad = (lng * Math.PI) / 180;

  return \`(
    6371000 * 2 * ASIN(
      SQRT(
        POWER(SIN((capsule.latitude::float * PI() / 180 - \${latRad}) / 2), 2) +
        COS(\${latRad}) * 
        COS(capsule.latitude::float * PI() / 180) * 
        POWER(SIN((capsule.longitude::float * PI() / 180 - \${lngRad}) / 2), 2)
      )
    )
  )\`;
}

// 사용 예시
async findNearby(user: User, query: GetCapsulesListQueryDto) {
  const { lat, lng, radius_m = 300 } = query;

  const qb = this.capsuleRepository
    .createQueryBuilder('capsule')
    .where(\`\${this.buildDistanceExpr(lat, lng)} <= :radius_m\`, { radius_m })
    .getMany();

  // ...
}
\`\`\`

---

## 💡 기술적 인사이트

### 1. **Haversine 공식**
- 구면 삼각법 기반 거리 계산
- 정확도: 짧은 거리(~수백 km)에서 매우 정확

### 2. **최적화 고려 사항**
- **공간 인덱스**: PostGIS 확장 사용 시 성능 개선 가능
- **바운딩 박스 프리필터**: 정사각형 영역 먼저 필터링 후 정확한 거리 계산

### 3. **위도/경도 검증**
\`\`\`typescript
if (lat < -90 || lat > 90) {
  throw new BadRequestException('LATITUDE_OUT_OF_RANGE');
}
if (lng < -180 || lng > 180) {
  throw new BadRequestException('LONGITUDE_OUT_OF_RANGE');
}
\`\`\`

---

# 5. 주문 금액 계산 로직

## 🎯 기술적 도전 과제

### 문제 상황
- **복잡한 가격 정책**:
  - 기간별 요금 (1주 ~ 3년)
  - 인원 × 사진 × 500원
  - 인원 × 음악 추가 × 1,000원
  - 인원 × 동영상 추가 × 2,000원
- **동적 기간 계산** (CUSTOM 옵션)

### 해결 방법
- 단가 기반 계산 로직
- 100원 단위 반올림

---

## 🔑 핵심 코드

**파일**: `src/orders/orders.service.ts`

\`\`\`typescript
private calculateTimeOptionAmount(dto: CreateOrderDto): number {
  const week = 1000;
  const month = 5000;
  const year = 10000;
  const maxStorageYears = 5;
  const maxStorageDays = maxStorageYears * 365;

  switch (dto.time_option) {
    case TimeOption.ONE_WEEK:
      return week;
    case TimeOption.ONE_MONTH:
      return month;
    case TimeOption.ONE_YEAR:
      return year;
    case TimeOption.TWO_YEAR:
      return year * 2;
    case TimeOption.THREE_YEAR:
      return year * 3;
    case TimeOption.CUSTOM: {
      const openAt = new Date(dto.custom_open_at!);
      const ms = openAt.getTime() - Date.now();
      const days = Math.ceil(ms / (1000 * 60 * 60 * 24));
      
      if (days > maxStorageDays) {
        throw new BadRequestException('STORAGE_PERIOD_EXCEEDED');
      }
      
      // 연 단가를 일 단가로 환산
      const perDay = year / 365;
      const dynamic = this.roundToHundred(perDay * days);
      return Math.max(week, dynamic); // 최소 1주 요금 보장
    }
    default:
      return week;
  }
}

async create(user: User, dto: CreateOrderDto) {
  const timeOptionAmount = this.calculateTimeOptionAmount(dto);
  const photoCount = dto.photo_count ?? 0;
  const imageAmount = dto.headcount * photoCount * 500;
  const audioAmount = dto.add_music ? dto.headcount * 1000 : 0;
  const videoAmount = dto.add_video ? dto.headcount * 2000 : 0;

  const totalAmount = timeOptionAmount + imageAmount + audioAmount + videoAmount;

  // ...
}
\`\`\`

---

## 💡 기술적 인사이트

### 1. **100원 단위 반올림**
\`\`\`typescript
private roundToHundred(amount: number): number {
  return Math.round(amount / 100) * 100;
}
\`\`\`

### 2. **가격 정책 예시**
| 기간 | 기본 요금 |
|------|----------|
| 1주  | 1,000원  |
| 1개월 | 5,000원 |
| 1년  | 10,000원 |
| 2년  | 20,000원 |
| 3년  | 30,000원 |

| 옵션 | 단가 (인당) |
|------|-------------|
| 사진 1장 | 500원 |
| 음악 추가 | 1,000원 |
| 동영상 추가 | 2,000원 |

---

## 🎯 발표 시 강조할 포인트

### 1. **결제-캡슐 생성 트랜잭션** (가장 추천!)
- 분산 트랜잭션 문제와 보상 처리
- 실무에서 자주 겪는 문제
- 에러 핸들링 전략

### 2. **동시성 제어**
- Pessimistic Lock으로 Race Condition 방지
- 트랜잭션 격리 수준

### 3. **스케줄링 자동화**
- Cron 기반 비즈니스 로직
- 알림 통합

### 4. **지리 계산 알고리즘**
- Haversine 공식의 수학적 배경
- 실시간 거리 계산

### 5. **복잡한 비즈니스 규칙**
- 동적 가격 계산
- 검증 로직

---

## 📝 PPT 구성 제안

### 슬라이드 구성 (15~20장)

1. **표지** - 프로젝트 소개
2. **문제 정의** - 왜 이 기능을 구현했는가?
3. **기술 선택** - NestJS, TypeORM, 토스페이먼츠
4. **플로우 차트** - 전체 흐름
5. **핵심 코드 1** - 트랜잭션 메인 로직
6. **핵심 코드 2** - 보상 트랜잭션
7. **시퀀스 다이어그램** - 실패 시나리오 포함
8. **기술적 도전 과제** - 해결 과정
9. **테스트 결과** - 실제 동작 화면
10. **배운 점 & 개선 방향**

### 시각 자료
- 플로우 차트: Mermaid, Draw.io, Excalidraw
- 코드 하이라이트: Carbon.now.sh, Ray.so
- 다이어그램: PlantUML, Mermaid

---

## 🚀 다음 단계

1. **이 문서를 바탕으로 PPT 작성**
2. **실제 동작 화면 캡처** (Postman, 브라우저)
3. **발표 연습** (5~10분 분량)

기술 발표 화이팅! 🎉
