# 🎯 Banny-Banny 핵심 시퀀스 다이어그램

> **프로젝트**: Banny-Banny 타임캡슐 서비스 백엔드  
> **작성일**: 2026-01-12  
> **목적**: 주요 비즈니스 플로우 시각화

---

## 목차
1. [결제 및 타임캡슐 생성 플로우](#1-결제-및-타임캡슐-생성-플로우)
2. [보상 트랜잭션 (결제 실패 처리)](#2-보상-트랜잭션-결제-실패-처리)
3. [Step Room 참여 및 슬롯 배정](#3-step-room-참여-및-슬롯-배정)
4. [이스터에그 조회 및 권한 검증](#4-이스터에그-조회-및-권한-검증)
5. [카카오 OAuth 로그인 플로우](#5-카카오-oauth-로그인-플로우)
6. [이스터에그 발견 기록](#6-이스터에그-발견-기록)

---

## 1. 결제 및 타임캡슐 생성 플로우

### 📝 설명
토스페이먼츠 결제 승인 후 타임캡슐과 Step Room을 생성하는 전체 플로우입니다.

```mermaid
sequenceDiagram
    participant Client as 클라이언트
    participant API as NestJS API
    participant Toss as 토스페이먼츠
    participant DB as PostgreSQL
    
    Client->>API: POST /payments/toss/confirm
    Note over Client,API: {orderId, paymentKey, amount}
    
    activate API
    API->>DB: 1. 주문 조회 및 검증
    DB-->>API: Order 정보 반환
    
    alt 주문 검증 실패
        API-->>Client: 400 Bad Request
        Note over Client,API: ORDER_NOT_FOUND<br/>AMOUNT_MISMATCH<br/>ORDER_ALREADY_PAID
    end
    
    API->>Toss: 2. 결제 승인 요청
    Note over API,Toss: POST /v1/payments/confirm<br/>{paymentKey, orderId, amount}
    
    activate Toss
    Toss-->>API: 승인 성공
    Note over Toss,API: {status: "DONE", approvedAt}
    deactivate Toss
    
    API->>DB: 3. DB 트랜잭션 시작
    activate DB
    API->>DB: 3-1. Payment 엔티티 생성/업데이트
    Note over API,DB: status = PAID<br/>pgTid = paymentKey
    API->>DB: 3-2. Order 상태 업데이트
    Note over API,DB: status = PAID
    DB-->>API: 트랜잭션 커밋
    deactivate DB
    
    API->>DB: 4. 캡슐 생성 (Step Room)
    activate DB
    API->>DB: 4-1. Capsule 엔티티 생성
    Note over API,DB: type = TIME_CAPSULE<br/>inviteCode 생성<br/>roomStatus = WAITING
    API->>DB: 4-2. ParticipantSlots 생성
    Note over API,DB: headcount만큼 슬롯 생성
    DB-->>API: Capsule 반환
    deactivate DB
    
    API-->>Client: 200 OK
    Note over Client,API: {<br/>  order_id,<br/>  payment_key,<br/>  capsule_id,<br/>  invite_code<br/>}
    deactivate API
```

### 🔑 핵심 포인트
- **2단계 커밋 패턴**: PG 승인 → DB 저장 → 캡슐 생성
- **트랜잭션 관리**: 각 단계별 독립적인 트랜잭션
- **멱등성 보장**: 동일 orderId로 중복 승인 방지

---

## 2. 보상 트랜잭션 (결제 실패 처리)

### 📝 설명
결제는 성공했지만 DB 저장 또는 캡슐 생성 실패 시 자동 환불 처리

```mermaid
sequenceDiagram
    participant Client as 클라이언트
    participant API as NestJS API
    participant Toss as 토스페이먼츠
    participant DB as PostgreSQL
    
    Client->>API: POST /payments/toss/confirm
    activate API
    
    API->>Toss: 1. 결제 승인 요청
    activate Toss
    Toss-->>API: ✅ 승인 성공
    Note over Toss,API: paymentKey 발급
    deactivate Toss
    
    API->>DB: 2. DB 트랜잭션 시작
    activate DB
    
    rect rgb(255, 200, 200)
        Note over API,DB: ❌ DB 저장 실패<br/>(네트워크 오류, 제약 조건 위반 등)
        DB--xAPI: Error: Transaction Failed
    end
    deactivate DB
    
    rect rgb(255, 230, 150)
        Note over API: 🔄 보상 트랜잭션 시작
        API->>Toss: 3. 환불 요청 (자동)
        Note over API,Toss: POST /v1/payments/{paymentKey}/cancel<br/>{<br/>  cancelReason: "결제 처리 실패",<br/>  cancelAmount<br/>}
        
        activate Toss
        Toss-->>API: ✅ 환불 완료
        Note over Toss,API: {status: "CANCELED"}
        deactivate Toss
    end
    
    API->>API: 4. 에러 로그 기록
    Note over API: console.error(<br/>  "보상 트랜잭션 실행",<br/>  {orderId, paymentKey}<br/>)
    
    API-->>Client: 500 Internal Server Error
    Note over Client,API: {<br/>  message: "결제 처리 중 오류",<br/>  error: "PAYMENT_PROCESSING_FAILED"<br/>}
    deactivate API
    
    rect rgb(200, 255, 200)
        Note over Client: ✅ 사용자는 재시도 가능<br/>(환불 완료되어 중복 결제 없음)
    end
```

### 🔑 핵심 포인트
- **Saga 패턴**: 분산 트랜잭션 보상 처리
- **자동 환불**: 실패 시 즉시 토스페이먼츠 API 호출
- **사용자 보호**: 중복 결제 방지, 재시도 가능

---

## 3. Step Room 참여 및 슬롯 배정

### 📝 설명
초대 코드로 타임캡슐 대기실에 참여하고 슬롯을 배정받는 플로우

```mermaid
sequenceDiagram
    participant User1 as 사용자 A
    participant User2 as 사용자 B
    participant API as NestJS API
    participant DB as PostgreSQL (with Lock)
    
    Note over User1,User2: 두 사용자가 동시에 참여 시도
    
    par 동시 요청
        User1->>API: POST /capsules/step-room/join
        Note over User1,API: {invite_code: "ABC123"}
    and
        User2->>API: POST /capsules/step-room/join
        Note over User2,API: {invite_code: "ABC123"}
    end
    
    activate API
    rect rgb(255, 240, 200)
        Note over API: 사용자 A 트랜잭션 시작 (먼저 도착)
        API->>DB: 1. 캡슐 조회
        DB-->>API: Capsule 반환
        
        API->>API: 2. 초대 코드 검증
        Note over API: inviteCode === "ABC123"
        
        API->>API: 3. 마감시한 확인
        Note over API: now < deadline
        
        API->>DB: 4. 슬롯 조회 (🔒 Pessimistic Write Lock)
        Note over DB: SELECT ... FOR UPDATE
        activate DB
        DB-->>API: Slots [0, 1, 2, 3] 반환
        
        API->>API: 5. 이미 참여 중인지 확인
        Note over API: 기존 슬롯에 userId 없음
        
        API->>API: 6. 빈 슬롯 찾기
        Note over API: Slot[0].userId = null ✅
        
        API->>DB: 7. 슬롯 배정
        Note over DB: Slot[0].userId = "user-A-id"<br/>assignedAt = now()
        DB-->>API: 저장 완료
        deactivate DB
    end
    
    API-->>User1: 200 OK
    Note over User1,API: {<br/>  room_id,<br/>  slot_number: 1,<br/>  joined_at<br/>}
    
    rect rgb(240, 255, 240)
        Note over API: 사용자 B 트랜잭션 시작 (대기 중이었음)
        API->>DB: 1-4. 동일 과정 (Lock 대기 후 실행)
        
        activate DB
        DB-->>API: Slots [0(user-A), 1, 2, 3] 반환
        
        API->>API: 6. 빈 슬롯 찾기
        Note over API: Slot[1].userId = null ✅
        
        API->>DB: 7. 슬롯 배정
        Note over DB: Slot[1].userId = "user-B-id"
        DB-->>API: 저장 완료
        deactivate DB
    end
    
    API-->>User2: 200 OK
    Note over User2,API: {<br/>  slot_number: 2<br/>}
    deactivate API
    
    rect rgb(200, 255, 200)
        Note over User1,User2: ✅ 두 사용자 모두 다른 슬롯 배정<br/>(Race Condition 방지)
    end
```

### 🔑 핵심 포인트
- **Pessimistic Lock**: `SELECT ... FOR UPDATE`로 동시성 제어
- **Race Condition 방지**: 트랜잭션 격리로 슬롯 충돌 없음
- **순차 처리**: 먼저 도착한 요청이 Lock 획득, 나머지는 대기

---

## 4. 이스터에그 조회 및 권한 검증

### 📝 설명
사용자가 이스터에그를 조회할 때 위치, 친구 관계, 조회 제한을 검증하는 플로우

```mermaid
sequenceDiagram
    participant Client as 클라이언트
    participant API as NestJS API
    participant AuthGuard as JWT Guard
    participant DB as PostgreSQL
    
    Client->>API: GET /capsules/:id?lat=37.5&lng=127.0
    Note over Client,API: Authorization: Bearer {token}
    
    activate API
    API->>AuthGuard: 1. JWT 토큰 검증
    activate AuthGuard
    AuthGuard->>AuthGuard: 토큰 디코드
    AuthGuard-->>API: ✅ User 정보 반환
    deactivate AuthGuard
    
    API->>DB: 2. 캡슐 조회
    activate DB
    DB-->>API: Capsule 반환
    deactivate DB
    
    alt 캡슐 미존재
        API-->>Client: 404 Not Found
        Note over Client,API: CAPSULE_NOT_FOUND
    end
    
    rect rgb(240, 248, 255)
        Note over API: 권한 검증 시작
        
        alt 본인 캡슐인가?
            API->>API: capsule.authorId === user.id?
            Note over API: ✅ 본인 캡슐 → 즉시 반환
            API-->>Client: 200 OK (캡슐 데이터)
        else 타인 캡슐
            API->>API: 3. 위치 검증 (Haversine 공식)
            Note over API: distance = haversine(capsule.lat/lng, user.lat/lng)
            
            alt 300m 초과
                API-->>Client: 403 Forbidden
                Note over Client,API: 위치 미도달 (distance: 450m)
            end
            
            API->>DB: 4. 친구 관계 조회
            activate DB
            Note over DB: SELECT * FROM friendships<br/>WHERE status = 'CONNECTED'<br/>AND (A->B OR B->A)
            DB-->>API: Friendship 반환
            deactivate DB
            
            alt 친구 아님
                API-->>Client: 403 Forbidden
                Note over Client,API: 친구 관계가 아닙니다
            end
            
            API->>API: 5. 조회 제한 확인
            Note over API: viewCount < viewLimit?
            
            alt 조회 제한 소진
                API-->>Client: 403 Forbidden
                Note over Client,API: 조회 제한 소진 (3/3)
            end
            
            Note over API: ✅ 모든 검증 통과
            API-->>Client: 200 OK (캡슐 데이터)
        end
    end
    deactivate API
```

### 🔑 핵심 포인트
- **다중 검증 레이어**: 인증 → 본인 여부 → 위치 → 친구 → 제한
- **Haversine 공식**: 실제 지구 곡률을 고려한 거리 계산
- **조기 반환**: 본인 캡슐은 검증 스킵으로 성능 최적화

---

## 5. 카카오 OAuth 로그인 플로우

### 📝 설명
카카오 소셜 로그인 및 회원가입 통합 플로우

```mermaid
sequenceDiagram
    participant User as 사용자
    participant Client as 프론트엔드
    participant API as NestJS API
    participant Kakao as 카카오 서버
    participant DB as PostgreSQL
    
    User->>Client: 1. "카카오 로그인" 버튼 클릭
    Client->>API: GET /auth/kakao
    
    activate API
    API-->>Client: 302 Redirect
    Note over Client,API: → 카카오 로그인 페이지
    deactivate API
    
    Client->>Kakao: 2. 리다이렉트
    activate Kakao
    
    User->>Kakao: 3. 카카오 계정 입력
    Note over User,Kakao: 이메일/비밀번호
    
    Kakao->>Kakao: 4. 인증 처리
    
    Kakao-->>Client: 5. 인가 코드 발급
    Note over Kakao,Client: 302 Redirect<br/>→ /auth/kakao/callback?code=xxx
    deactivate Kakao
    
    Client->>API: GET /auth/kakao/callback?code=xxx
    activate API
    
    API->>Kakao: 6. 액세스 토큰 요청
    Note over API,Kakao: POST /oauth/token<br/>{code, client_id, client_secret}
    activate Kakao
    Kakao-->>API: 액세스 토큰 반환
    Note over Kakao,API: {access_token, refresh_token}
    deactivate Kakao
    
    API->>Kakao: 7. 사용자 정보 조회
    Note over API,Kakao: GET /v2/user/me<br/>Authorization: Bearer {access_token}
    activate Kakao
    Kakao-->>API: 사용자 프로필 반환
    Note over Kakao,API: {<br/>  id: 123456789,<br/>  kakao_account: {<br/>    email,<br/>    profile: {nickname, profile_image}<br/>  }<br/>}
    deactivate Kakao
    
    API->>DB: 8. 사용자 조회 (카카오 ID)
    activate DB
    DB-->>API: User 반환 (or null)
    deactivate DB
    
    alt 신규 회원
        API->>DB: 9-1. 회원가입 (User 생성)
        activate DB
        Note over DB: INSERT INTO users<br/>{<br/>  kakao_id,<br/>  email,<br/>  nickname,<br/>  profile_img<br/>}
        DB-->>API: 신규 User 반환
        deactivate DB
    else 기존 회원
        API->>DB: 9-2. 로그인 정보 업데이트
        activate DB
        Note over DB: UPDATE users<br/>SET last_login_at = now()
        DB-->>API: 기존 User 반환
        deactivate DB
    end
    
    API->>API: 10. JWT 토큰 생성
    Note over API: jwt.sign({<br/>  userId: user.id,<br/>  email: user.email<br/>}, SECRET)
    
    API-->>Client: 302 Redirect
    Note over Client,API: → /login/success?token=xxx
    deactivate API
    
    Client->>Client: 11. 토큰 저장
    Note over Client: localStorage.setItem(<br/>  'accessToken',<br/>  token<br/>)
    
    Client-->>User: 12. 홈 화면으로 이동
    Note over User,Client: ✅ 로그인 완료
```

### 🔑 핵심 포인트
- **OAuth 2.0 Authorization Code Flow**: 표준 인증 플로우
- **자동 회원가입**: 신규 사용자 자동 생성 (회원가입 화면 불필요)
- **JWT 토큰 발급**: 이후 API 호출에 사용

---

## 6. 이스터에그 발견 기록

### 📝 설명
사용자가 이스터에그를 발견했을 때 조회 로그를 기록하는 플로우

```mermaid
sequenceDiagram
    participant Client as 클라이언트
    participant API as NestJS API
    participant DB as PostgreSQL
    
    Client->>API: POST /capsules/:id/viewers
    Note over Client,API: Authorization: Bearer {token}
    
    activate API
    API->>DB: 1. 캡슐 조회
    activate DB
    DB-->>API: Capsule 반환
    deactivate DB
    
    alt 캡슐 미존재
        API-->>Client: 404 Not Found
    end
    
    API->>DB: 2. 기존 조회 로그 확인
    activate DB
    Note over DB: SELECT * FROM capsule_access_logs<br/>WHERE capsule_id = :id<br/>AND user_id = :userId
    DB-->>API: AccessLog 반환 (or null)
    deactivate DB
    
    alt 이미 발견한 적 있음
        API-->>Client: 200 OK
        Note over Client,API: {<br/>  success: true,<br/>  message: "이미 발견한 이스터에그",<br/>  is_first_view: false<br/>}
    else 최초 발견
        API->>DB: 3. 트랜잭션 시작
        activate DB
        
        API->>DB: 3-1. 조회 로그 생성
        Note over DB: INSERT INTO capsule_access_logs<br/>{<br/>  capsule_id,<br/>  user_id,<br/>  viewed_at: now()<br/>}
        
        API->>DB: 3-2. 조회 카운트 증가
        Note over DB: UPDATE capsules<br/>SET view_count = view_count + 1<br/>WHERE id = :capsuleId
        
        DB-->>API: 트랜잭션 커밋
        deactivate DB
        
        rect rgb(255, 250, 200)
            Note over API: 🎉 첫 발견 보상 처리 (예정)
            Note over API: - 배지 지급<br/>- 포인트 적립<br/>- 알림 전송
        end
        
        API-->>Client: 201 Created
        Note over Client,API: {<br/>  success: true,<br/>  message: "이스터에그를 발견했습니다!",<br/>  is_first_view: true,<br/>  rewards: {<br/>    badge: "첫 발견",<br/>    points: 10<br/>  }<br/>}
    end
    deactivate API
    
    Client->>Client: UI 업데이트
    Note over Client: ✨ 발견 애니메이션<br/>📊 마이페이지 업데이트
```

### 🔑 핵심 포인트
- **중복 방지**: 동일 사용자의 중복 조회 카운트 방지
- **트랜잭션**: 로그 생성 + 카운트 증가 원자성 보장
- **확장 가능**: 보상 시스템 추가 예정 (배지, 포인트)

---

## 📊 아키텍처 전체 플로우

```mermaid
graph TB
    subgraph "클라이언트 레이어"
        A[React/Next.js]
        B[Kakao SDK]
    end
    
    subgraph "API 게이트웨이"
        C[NestJS Controller]
        D[JWT Auth Guard]
    end
    
    subgraph "비즈니스 로직"
        E[Service Layer]
        F[결제 처리]
        G[권한 검증]
        H[위치 계산]
    end
    
    subgraph "데이터 레이어"
        I[TypeORM Repository]
        J[PostgreSQL]
    end
    
    subgraph "외부 서비스"
        K[토스페이먼츠 API]
        L[카카오 OAuth]
        M[AWS S3]
    end
    
    A --> C
    B --> L
    C --> D
    D --> E
    E --> F
    E --> G
    E --> H
    E --> I
    F --> K
    G --> I
    I --> J
    E --> M
    
    style A fill:#e1f5ff
    style C fill:#fff4e1
    style E fill:#ffe1f5
    style I fill:#e1ffe1
    style K fill:#ffe1e1
    style L fill:#ffe1e1
```

---

## 🎯 다이어그램 사용 가이드

### Mermaid 렌더링 방법

#### 1. **GitHub / GitLab**
- 마크다운 파일에 그대로 푸시하면 자동 렌더링

#### 2. **VS Code**
```bash
# Mermaid Preview 확장 설치
ext install bierner.markdown-mermaid
```
- Ctrl/Cmd + Shift + V로 미리보기

#### 3. **온라인 에디터**
- [Mermaid Live Editor](https://mermaid.live/)
- 코드 붙여넣기 → PNG/SVG 다운로드

#### 4. **Notion / Obsidian**
- Mermaid 블록 지원 (바로 임베드 가능)

---

## 💡 발표 시 활용 팁

### 각 다이어그램의 강조 포인트

| 다이어그램 | 강조할 내용 | 시간 |
|-----------|-------------|------|
| **결제-캡슐 생성** | 분산 트랜잭션, 2단계 커밋 | 2분 |
| **보상 트랜잭션** | Saga 패턴, 자동 환불 | 2분 |
| **Step Room** | 동시성 제어, Pessimistic Lock | 1.5분 |
| **권한 검증** | 다중 레이어 검증, 보안 | 1.5분 |
| **OAuth** | 표준 인증 플로우 | 1분 |

### 발표 스크립트 예시

> "이 다이어그램은 결제와 캡슐 생성의 전체 플로우입니다. 
> 
> 특히 **보상 트랜잭션** 부분을 주목해주세요. PG 승인은 성공했지만 DB 저장이 실패하면, 자동으로 환불 API를 호출합니다. 이는 **Saga 패턴**을 적용한 것으로, 분산 시스템에서 데이터 일관성을 보장하는 핵심 기법입니다.
>
> 사용자는 에러를 받더라도 중복 결제 걱정 없이 재시도할 수 있습니다."

---

## 🎨 커스터마이징

### 색상 테마 변경

```mermaid
%%{init: {'theme':'forest'}}%%
sequenceDiagram
    participant A
    participant B
    A->>B: 메시지
```

**테마 옵션**: `default`, `dark`, `forest`, `neutral`

### 노트 강조

```mermaid
sequenceDiagram
    Note over A,B: ⚠️ 중요!
    rect rgb(255, 200, 200)
        Note over A: ❌ 실패 시나리오
    end
    rect rgb(200, 255, 200)
        Note over B: ✅ 성공 케이스
    end
```

---

## 📚 참고 자료

- [Mermaid 공식 문서](https://mermaid.js.org/)
- [Sequence Diagram Syntax](https://mermaid.js.org/syntax/sequenceDiagram.html)
- [Draw.io 대안 비교](https://www.diagrams.net/)

---

**작성일**: 2026-01-12  
**프로젝트**: Banny-Banny Backend  
**다이어그램 수**: 6개 (+ 1개 아키텍처 그래프)
